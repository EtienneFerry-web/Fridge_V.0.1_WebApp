<footer class="bg-dark text-white mt-auto py-5">
    <div class="container">
        <div class="row gy-4">
            <div class="col-md-4 text-center text-md-start">
                <h5 class="fw-bold mb-2">FRIDGE</h5>
                <p class="small mb-0 text-secondary">Cuisinez intelligemment avec ce que vous avez sous la main.</p>
            </div>

            <div class="col-md-4 text-center">
                <h6 class="text-uppercase fw-bold mb-3 small">Navigation</h6>
                <ul class="list-unstyled d-flex flex-wrap justify-content-center gap-3 mb-0">
                    <li><a href="{{ path('home') }}" class="text-white-50 text-decoration-none small">Accueil</a></li>
                    <li><a href="{{ path('recipe_search') }}" class="text-white-50 text-decoration-none small">Recettes</a></li>
                    
                    {# Affichage conditionnel des outils de gestion #}
                    {% if app.user %}
                        <li><a href="{{ path('planning') }}" class="text-white-50 text-decoration-none small">Planning</a></li>
                        <li><a href="{{ path('stock') }}" class="text-white-50 text-decoration-none small">Stock</a></li>
                        <li><a href="{{ path('courses') }}" class="text-white-50 text-decoration-none small">Courses</a></li>
                    {% endif %}
                </ul>
            </div>

            <div class="col-md-4 text-center text-md-end">
                <h6 class="text-uppercase fw-bold mb-3 small">Liens utiles</h6>
                <div class="mb-2">
                    {% if app.user %}
                        <a href="{{ path('user_profile') }}" class="text-white-50 text-decoration-none small px-2">Mon Compte</a>
                    {% else %}
                        <a href="{{ path('login') }}" class="text-white-50 text-decoration-none small px-2">Connexion / Inscription</a>
                    {% endif %}
                    
                    <a href="#" class="text-white-50 text-decoration-none small px-2">Mentions légales</a>
                    <a href="#" class="text-white-50 text-decoration-none small px-2">Contact</a>
                </div>
                <div class="small text-white-50 mt-3">© 2026 FRIDGE. Tous droits réservés.</div>
            </div>
        </div>
    </div>
</footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="assets/script.js"></script>
</body>
</html>