<nav class="navbar navbar-expand-lg sticky-top" id="mainNav">
    <div class="container">

        <!-- Logo -->
        <a class="navbar-brand d-flex align-items-center gap-2" href="{{ path('app_home') }}">
            <div style="width:34px;height:34px;background:var(--primary);border-radius:10px;display:flex;align-items:center;justify-content:center;">
                <img src="{{ asset('img/fridge_favicon_nobg.png') }}" width="22" height="22" alt="Logo" style="filter:brightness(10);">
            </div>
            <span style="font-family:var(--font-display);font-weight:800;font-size:1.2rem;color:var(--text);letter-spacing:-0.03em;">
                Fridge
            </span>
        </a>

        <!-- Search Bar — centre (desktop) -->
        <div class="d-none d-lg-flex flex-grow-1 mx-5" style="max-width:480px;">
            <form action="{{ path('app_recette_index') }}" method="GET" class="w-100 position-relative">
                <i class="bi bi-search position-absolute top-50 translate-middle-y ms-3"
                   style="color:var(--text-muted);font-size:0.95rem;pointer-events:none;left:0;"></i>
                <input type="text" name="q"
                       class="form-control ps-5 border-0 shadow-none"
                       placeholder="Rechercher une recette…"
                       style="height:46px;background:var(--surface-muted);border-radius:var(--radius-xl);font-size:0.9rem;">
            </form>
        </div>

        <!-- Toggler mobile -->
        <button class="navbar-toggler border-0 shadow-none ms-auto me-2" type="button"
                data-bs-toggle="collapse" data-bs-target="#navbarMain"
                aria-controls="navbarMain" aria-expanded="false" aria-label="Menu">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Nav items -->
        <div class="collapse navbar-collapse" id="navbarMain">

            <!-- Search mobile -->
            <div class="d-lg-none py-3 border-bottom" style="border-color:var(--border-light)!important;">
                <form action="{{ path('app_recette_index') }}" method="GET" class="position-relative">
                    <i class="bi bi-search position-absolute top-50 translate-middle-y ms-3"
                       style="color:var(--text-muted);font-size:0.9rem;pointer-events:none;left:0;"></i>
                    <input type="text" name="q"
                           class="form-control ps-5 border-0 shadow-none"
                           placeholder="Rechercher…"
                           style="background:var(--surface-muted);border-radius:var(--radius-xl);height:44px;">
                </form>
            </div>

            <ul class="navbar-nav ms-auto align-items-lg-center gap-1 py-2 py-lg-0">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'app_home' %}active{% endif %}"
                       href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') starts with 'app_recette' %}active{% endif %}"
                       href="{{ path('app_recette_index') }}">Découvrir</a>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_planning' %}active{% endif %}"
                           href="{{ path('app_planning') }}">Planning</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_liste_course_index' %}active{% endif %}"
                           href="{{ path('app_liste_course_index') }}">Courses</a>
                    </li>

                    <!-- Avatar dropdown -->
                    <li class="nav-item dropdown ms-lg-2 mt-2 mt-lg-0">
                        <a class="nav-link dropdown-toggle d-flex align-items-center gap-2 p-0" href="#"
                           id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <div style="width:38px;height:38px;background:var(--primary-light);border:2px solid var(--primary);border-radius:50%;display:flex;align-items:center;justify-content:center;">
                                <i class="bi bi-person-fill" style="color:var(--primary);font-size:1.05rem;"></i>
                            </div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end mt-2" style="min-width:220px;">
                            <li class="px-3 py-2">
                                <p class="mb-0 fw-700" style="font-size:0.85rem;color:var(--text);">{{ app.user.userIdentifier }}</p>
                                <p class="mb-0" style="font-size:0.78rem;color:var(--text-secondary);">Mon compte</p>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_user_profil') }}"><i class="bi bi-person me-2 text-muted"></i>Profil</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_planning') }}"><i class="bi bi-calendar-week me-2 text-muted"></i>Mon Planning</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_stock') }}"><i class="bi bi-box-seam me-2 text-muted"></i>Mon Inventaire</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_liste_course_index') }}"><i class="bi bi-basket me-2 text-muted"></i>Mes Courses</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_logout') }}"
                                   style="color:var(--danger);">
                                    <i class="bi bi-box-arrow-right me-2"></i>Déconnexion
                                </a>
                            </li>
                        </ul>
                    </li>

                {% else %}
                    <li class="nav-item ms-lg-2 mt-2 mt-lg-0">
                        <a class="btn btn-primary rounded-pill px-4" href="{{ path('app_login') }}"
                           style="min-height:40px;font-size:0.88rem;">
                            Connexion
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
